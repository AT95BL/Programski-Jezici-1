#include "Vector3D.h"
#include "Vector4D.h"
#include "Vector2D.h"
#include "DynamicArray.h"
#include "InsertBufferedArray.h"

int main()
{
	/* 
	Vector3D i(1, 0, 0), j(0, 1, 0), k(0, 0, 1), o = o.zeroPoint(o);
	(i * 2).printSomething();
	(2 * i).printSomething();
	(i + j + k).printSomething();
	o.printSomething();
	std::cout << i * i << std::endl;
	*/

	/* 
	Vector4D i(1, 0, 0, 0), j(0, 1, 0, 0), k(0, 0, 1, 0), h(0, 0, 0, 1), o = o.zeroPoint(o);
	(i * 2).printSomething();
	(2 * i).printSomething();
	(i + j + k + h).printSomething();
	o.printSomething();
	std::cout << i * i << std::endl;
	*/

	/* 
	Vector2D i(1, 0), j(0, 1), o = o.zeroPoint(o);
	(i * 2).printSomething();
	(2 * i).printSomething();
	(i + j).printSomething();
	o.printSomething();
	std::cout << i * j << std::endl;
	*/

	
	int niz[10] = { 1, 2, 3, 4, 5 };

	
}